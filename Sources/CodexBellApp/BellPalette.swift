#if os(macOS)
import SwiftUI

enum BellPalette {
    static let purple = Color(red: 0.55, green: 0.38, blue: 0.95)
    static let yellow = Color(red: 0.98, green: 0.74, blue: 0.18)
    static let controlTrack = Color.primary.opacity(0.16)
}
#endif
